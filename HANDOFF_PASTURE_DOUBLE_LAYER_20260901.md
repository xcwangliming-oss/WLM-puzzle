# 牧场双层消除：交接说明（2026-09-01）

## 工作位置与分支

- 工作树：`E:\Gemini\Puzzle_NewFeature_20260901`
- 分支：`feature/new-feature-20260901`
- 本地测试服：`http://127.0.0.1:5173/`
- 当前改动准备提交与部署；不要使用 `git reset --hard`、不要整仓回退。

## 功能目标（已确定）

这是一个独立玩法，位于“角色与道具样式”面板中的 **🐑 牧场双层消除**，不能替换或挤掉“自选双色”等既有玩法按钮。

启用后，普通绿色块使用三组按宽度（1~4 格）配置的素材：

1. **绿框草（初始块）**：绿色普通块的初始形态。
2. **草（第一层消除后）**：该块所在完整行第一次被消除时的中间形态。
3. **羊（第二层）**：羊从右侧进入并占用同一个块的位置。

后续再次消除羊所在的完整行时，羊随该行移除。树叶/羊毛粒子特效已按用户要求移除，后续由用户自行补充素材特效。

三组素材支持一次多选上传 1~4 张；文件名末尾的 `1` / `2` / `3` / `4`（可带“格”或 `x`）对应块宽。上传内容保存到 IndexedDB，刷新页面不应丢失。

## 当前关键状态：已恢复“第一版”的消除时序

用户多次反馈：后续为修复脚本/重力而加入的补丁造成“框草不变草、羊不出现”。用户要求回退到最初能出现羊的逻辑。

当前已**仅回退消除的时序归属**，保留以下较新的功能：

- 独立牧场面板
- 批量上传
- IndexedDB 素材持久化
- 存档/导出中保留牧场状态和素材
- 羊出现时保持上传序列帧播放
- 树叶和羊毛粒子特效已移除，后续再接用户自己的素材特效

恢复后的时序必须保持如下：

1. `checkEliminations()` 判断行已满后，**不要立即**调用 `advancePastureLayer()`。
2. 在该行的 GSAP 消除时间轴中，才调用 `advancePastureLayer(block)`。
3. `framed-grass` 与 `grass` 不加入物理移除列表；`sheep` 才会在下一次该行消除时物理移除。
4. 框草第一次进入时间轴时：先显示草，羊从右侧进入，抵达后设置为 `sheep`。
5. 羊第二次进入时间轴时：先缩小，再快速放大，然后淡出并按普通消除流程移除。

相关实现位置（`src/main.ts`）：

- `advancePastureLayer()`：约第 23384 行
- `sendSheepToPastureBlock()`：约第 23347 行
- `checkEliminations()` 的物理移除列表：约第 27334 行
- `checkEliminations()` 的牧场时间轴回调：约第 27654 行
- 独立面板与批量上传：约第 22154 行
- IndexedDB 保存/恢复：约第 930 行

**不要重新引入以下已撤掉的做法：**

- 在确认满行时直接 `pastureBlocksAdvancing.forEach(advancePastureLayer)`。
- 用 `pastureFirstLayerConversion` 提前中止重力或提前结束动画。
- 因脚本的 mode counter 变化而取消已经起飞的羊抵达回调。

这些方案均曾导致真实编辑器中“草不变、羊不出现”。

## 已完成验证

2026-09-01 本地验证结果：

- `node --test tests\\*.test.cjs`：39/39 通过。
- `npm.cmd run build`：通过。
- 本地浏览器 `?pasture-test` 的真实满行流程：
  - 第一次消行后状态为 `sheep`；
  - 第二次消行后对应块列表为空。

测试夹具只在带 `?pasture-test` 的本地 URL 生效，不影响正式编辑器：

- `http://127.0.0.1:5173/?pasture-test`

该夹具会为第二次验证把已下落到预览触发区域之外的羊移回可触发行；它只用于证明双层状态机，**不能替代用户在实际编辑器中使用上传素材的人工验收**。

## 下一位需要做什么

1. 让用户强制刷新 `http://127.0.0.1:5173/`（`Ctrl + F5`）。
2. 确认牧场开关已启用，且三组素材分别为：绿框草、无框草、羊；每张图对应正确 1~4 格宽。
3. 在用户真实编辑器流程中测试：
   - 首次完整行消除是否明显显示无框草并有羊从右侧进入；
   - 第二次完整行消除是否先缩小、快速放大、再移除羊；
   - 刷新后上传素材是否仍保留。
4. 若用户仍报告“未变化/无羊/卡顿”，先抓取**实际触发的入口与当时棋盘状态**（手动播放、脚本播放、录制回放或导入存档），不要继续凭静态代码猜测。

## 当前工作树改动

- `src/main.ts`：牧场双层消除、素材面板/持久化、状态保存、序列帧支持、羊交互修复、羊第二次消除缩小-放大-消失动画。
- `src/style.css`、`index.html`：此前为独立面板加入的界面改动，保留。
- `tests/pasture-layer-mode-regression.test.cjs`：牧场功能回归检查。
